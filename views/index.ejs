<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>A Smart Senior Projects Management System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <style>
    .hero-section {
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      color: white;
      border-radius: 15px;
      padding: 3rem 2rem;
      margin-top: 2rem;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }

    .auth-buttons .btn {
      min-width: 150px;
      margin: 0.5rem;
      transition: all 0.3s ease;
      font-weight: 500;
    }

    .btn-signin {
      background-color: white;
      color: #2575fc;
      border: 2px solid white;
    }

    .btn-signin:hover {
      background-color: transparent;
      color: white;
    }

    .btn-register {
      background-color: transparent;
      color: white;
      border: 2px solid white;
    }

    .btn-register:hover {
      background-color: white;
      color: #2575fc;
    }

    .system-title {
      font-weight: 700;
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>

<body class="bg-light">
  <%- include('./partials/_navbar.ejs') %>

    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
          <div class="hero-section text-center">
            <h1 class="system-title display-4 mb-4">A Smart Senior Projects Management System</h1>

            <% if (user) { %>
              <h1>Welcome to the app, <%= user.username %>!</h1>
              <p>
                <a href="/auth/sign-out">Sign out</a>
              </p>
              <div class="spinner-border text-light" role="status">
                <span class="visually-hidden">Loading...</span>
              </div>
              <p class="mt-3">Redirecting to your dashboard...</p>
              <% } else { %>
                <p class="lead mb-4">Manage your senior projects efficiently with our comprehensive system</p>
                <div class="auth-buttons d-flex flex-wrap justify-content-center">
                  <a href="/auth/sign-in" class="btn btn-signin btn-lg">
                    <i class="bi bi-box-arrow-in-right"></i> Sign In
                  </a>
                  <a href="/auth/sign-up" class="btn btn-register btn-lg">
                    <i class="bi bi-person-plus"></i> Register
                  </a>
                </div>
                <% } %>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>